- lista di mutazioni frequenti, secondarie, cancerogene, bystander
	- trova su cluster BAM files
	- crea BED file con mutations e usa deeptools e conti quante reads in quella regione mutata
	- prima di deeptools --> grep in cui separo BAM per wt e mutated e calcolo ratio 

- Improve train_models.py
	- In particular, improve feature selection + hyperparam tuning
	- BUT we need nested CV probably
	- 5 fold is better than 10 in this case because we have too few samples

- create decorrelation function
   - 10:1 observation feature cutoff for sampling
	-> see CpG tournaments
	-> 3:1, 5:1 ratios?

- automatized sample selection in PCA space (per giuoco)
- sample exclusion list sia per metiloma, che per trascrittoma (per obbligo)

- Automatic elbow finder
	top k more variable vs elbow

- improve feature selection
	in case of ties, which features?
	CpG tournaments

- create decorrelation function
	
- combined importance score

- imputation for missing CpGs
	- group CpGs (inside of associated genes) by distance from tss, correlation, TF-annotation?
		-> infer their importance

- add double Snakefile: safe because filtering works the same

- docker

- add conditional param in get_genome_for_GRN
- add conditional param in get_genome_for_GRN



REMEMBER TO INSTALL GIMME ENV
I first installed mamba in my base conda env
then I installed GIMME
then genomepy
then I manually inserted the env in .snakemake folder